apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: kubernetes-dashboard
  
spec:
  selector:
    k8s-app: kubernetes-dashboard 
  type: NodePort  
  ports:
    - port: 443
      targetPort: 8443 
      nodePort: 30002